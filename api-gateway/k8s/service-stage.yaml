apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: stage
  labels:
    app: api-gateway
    environment: stage
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
  - name: http
    protocol: TCP
    port: 9000
    targetPort: 9000

